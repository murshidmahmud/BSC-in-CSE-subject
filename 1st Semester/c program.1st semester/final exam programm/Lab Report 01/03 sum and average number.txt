// find the sum and average number in an array:


#include <stdio.h>

int main() {
   int size;
   printf("Enter your array size: ");
   scanf("%d", &size);
   
   int arr[size];
   
   for(int i = 0; i< size; i++)
   {
       printf("%d index: ", i);
       scanf("%d", &arr[i]);
   }
   
   //printf("Array elements is: ");
   //for(int i = 0; i<size; i++)
   //{
   //    printf("%d\t", arr[i]);
  // }
  
   //Sum and avarage element in an array: 

   int sum = 0, average;
   for(int i = 0; i < size; i++)
   {
       sum += arr[i];
       
   }
   average = sum / size;
   
   printf("\nArray elements of sum: %d", sum);
   printf("\nArray elements of average: %d", average);

    return 0;
}  