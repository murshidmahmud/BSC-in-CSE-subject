#include<stdio.h>

int check_UpperTriangular(int matrix[][60], int size){
    for(int i=1; i<size; i++){
        for(int k=0; k<size-2; k++){
            if(matrix[i][k] !=0){
                return 0;
            }
        }
    }
    return 1;
}
int main(){
    int size;
    printf("Enter the matrix size: ");
    scanf("%d",&size);
    
    int matrix[60][60];
    printf("Enter the element: \n");
    for(int i=0; i<size; i++){
        for(int k=0;k<size; k++){
            scanf("%d",&matrix[i][k]);
        }
    }
    
    if(check_UpperTriangular(matrix,size)){
        printf("The matrix is a uper traingular matrix.\n");
    }
    else{
        printf("The matrix isn't uper traingular matrix.");
    }
    return 0;
}